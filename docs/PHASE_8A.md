# Phase 8A：售卖状态与预约流程

本阶段新增统一前台售卖状态计算，按当前时间、开售时间、售卖截止时间和库存推导 `RESERVABLE`、`ON_SALE`、`SOLD_OUT`、`ENDED`、`UNAVAILABLE`。

完成内容：

- 演出详情页移除锁票时间、锁票提醒和开放库存数量。
- 未开售场次按钮显示“预约抢票”，进入购票预选页提交预约。
- 预约信息只保存场次、票档、数量和观演人，不下单、不扣库存、不锁座。
- 自主选座路由和锁座接口增加开售时间校验。

验证：

- `mvn -q -DskipTests package`
- `npm run build`

